/* 页面整体容器 */
.page {
    background: #f7f7f7;
    height: 100vh;
    overflow: hidden; /* 禁止页面自身滚动（避免双滚动） */
    display: flex;
    flex-direction: column;
  }
  
  /* 顶部搜索栏 */
  .lux-search-bar {
    display: flex;
    align-items: center;
    padding: 12px;
    background: #ffffff;
    z-index: 10; /* 保证浮在 suggest 上层 */
  }
  
  
  /* 搜索框外壳 */
  .lux-search-box {
    flex: 1;
    display: flex;
    align-items: center;
  
    background: #f7f8fa;
    border-radius: 30px;
  
    padding: 8px 14px;
    margin-right: 10px;
  
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.04); /* 瑞幸风轻阴影 */
  }
  
  /* 搜索图标 */
  .lux-search-icon {
    width: 16px;
    height: 16px;
    margin-right: 6px;
    display: block;
    opacity: 0.8;
  }
  
  /* 输入框 */
  .lux-search-input {
    flex: 1;
    font-size: 14px;
    color: #333;
  }
  
  /* 搜索按钮 */
  .lux-search-btn {
    padding: 6px 14px;
    background: #007aff;
    border-radius: 20px;
  
    font-size: 14px;
    font-weight: 300;
    color: #fff;
    line-height: 1;
    text-align: center;
  
    display: flex;
    align-items: center;
    justify-content: center;
  }
  

 /* 让 scroll-view 处于最低层 */

    /* 🔽 下拉浮层容器 */
  /* 让浮层在所有组件之上 */
  .suggest-list {
    position: absolute;
    top: 56px;
    left: 12px;
    right: 12px;
  
    background: #ffffff;
    border-radius: 10px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.08);
  
    z-index: 9999 !important;   /* 提到最顶层 */
    overflow: hidden;
    animation: fadeIn 0.15s ease-out;
  }
  
  
  /* 联想项 */
  .suggest-item {
    padding: 12px 14px;
    font-size: 14px;
    color: #333;
    border-bottom: 1px solid #f3f3f3;
  }
  
  .suggest-item:last-child {
    border-bottom: none;
  }
  
  /* 下拉动画 */
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(-4px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  /* 中间布局（左右两栏） */
  .layout {
    flex: 1;
    display: flex;
    height: 100%;
    overflow: hidden;
  }
  
  /* 左侧分类栏 */
  .sidebar {
    width: 180rpx;
    background: #f8f8f8;
    padding-top: 10rpx;
    padding-bottom: 10rpx;
  }
  
  /* 单个分类项 */
  .side-item {
    position: relative;
    padding: 10rpx 16rpx;
    margin-bottom: 4rpx;
    border-radius: 12rpx;
    min-height: 70rpx;
    display: flex;
    align-items: center;
    color: #666;
    transition: all 0.2s;
  }
  
  /* 选中分类项 */
  .side-item.active {
    background: #e6e6e6;
    color: #1c1c1f;
    font-weight: 600;
  }
  
  /* 左侧蓝色竖条 */
  .active-bar {
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 6rpx;
    height: 44rpx;
    background: #0056ff;
    border-radius: 100rpx;
    box-shadow: 0 0 12rpx rgba(0, 86, 255, 0.3);
  }
  
  /* 分类名称 */
  .name {
    font-size: 26rpx;
    text-align: center;
    margin-top: 8rpx;
    color: #666;
  }
  
  /* 分类项内部布局 */
  .name-box {
    position: relative;
    width: 100%;
    display: flex;
    flex-direction: column; /* badge 在上 */
    align-items: center;
    justify-content: center;
  }
  
  /* 新品、果珍图标 */
  .tag-new {
    width: 50rpx;
    position: absolute;
    top: -30rpx;
    right: -12rpx;
  }
  
  /* 右侧商品列表 */
  .goods-list {
    flex: 1;
    padding: 24rpx;
    position: relative;
    z-index: 0 !important;
  }
  
  /* 商品卡片 */
  .goods-card {
    background: white;
    border-radius: 24rpx;
    padding: 20rpx;
    margin-bottom: 28rpx;
    box-shadow: 0 8rpx 30rpx rgba(0, 0, 0, 0.05);
    display: flex;
  }
  
  /* 商品图片 */
  .goods-img {
    width: 180rpx;
    height: 180rpx;
    border-radius: 20rpx;
    background: #f2f2f2;
  }
  
  /* 商品信息区域 */
  .goods-info {
    flex: 1;
    margin-left: 24rpx;
  }
  
  /* 商品标题（瑞幸风） */
  .goods-title {
    font-size: 27rpx;
    font-weight: 600;
    color: #1c1c1e;
    line-height: 42rpx;
    margin-bottom: 8rpx;
  }
  
  /* 标签区域 */
  .goods-tags {
    display: flex;
    gap: 12rpx;
    margin-bottom: 8rpx;
  }
  
  .tag-item {
    background: #f5efe2;
    color: #b7985a;
    font-size: 22rpx;
    padding: 6rpx 16rpx;
    border-radius: 12rpx;
  }
  
  /* 描述 */
  .goods-desc {
    color: #7d7d7d;
    font-size: 24rpx;
    line-height: 32rpx;
    margin-bottom: 12rpx;
  }
  
  /* 单位 */
  .goods-price .unit {
    font-size: 26rpx;
    color: #d28a6a;
  }
  